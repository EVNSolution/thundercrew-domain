# dependencies
node_modules/

# Next.js
.next/
out/
*.tsbuildinfo
next-env.d.ts

# 빌드 시 node_modules 에서 복사되는 maplibre 워커 (scripts/copy-maplibre-worker.mjs).
# 번들러가 파일명에 해시를 붙이면 워커의 상대 import 가 깨지므로 정적으로 서빙한다.
development/frontend/public/maplibre/

# local env / secrets
.env
.env.local
.env.*.local
.env.production
.env.development
!.env.example

# Vercel local link metadata
.vercel/

# Supabase local temp
supabase/.temp/
**/supabase/.temp/

# OMX runtime/local agent state
.omx/

# CLEVER local control-plane workspace, not part of target product repo
clever-agent-workspace/

# OS/editor
.DS_Store
