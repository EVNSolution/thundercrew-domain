import { PageHeader } from "@/components/layout/PageHeader";
import { Badge } from "@/components/ui/Badge";
import { stations } from "@/lib/services/mock-data";
export default async function StationDetailPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const station = stations.find((s) => s.slug === slug) ?? stations[0]; return <div className="page-container"><PageHeader title={station.name} description={station.address} /><section className="content-grid"><div className="card"><h2>운영 정보</h2><div className="detail-list"><div className="detail-row"><span>상태</span><Badge>{station.status}</Badge></div><div className="detail-row"><span>보유 배터리</span><strong>{station.batteryCount}개</strong></div><div className="detail-row"><span>교체 가능</span><strong>{station.replaceableCount}개</strong></div><div className="detail-row"><span>mock 좌표</span><strong>{station.latitude}, {station.longitude}</strong></div></div><div className="form-actions"><button className="button-primary">재고 로그 등록</button></div></div><aside className="detail-panel"><h2>위치 카드</h2><p>민트 마커는 선택된 위치만 표시합니다. 지도 API 없이도 주소, 상태, 재고 중심 운영이 가능합니다.</p></aside></section></div>; }
