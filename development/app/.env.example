# Optional live API mode. Leave unset to use local mock services.
# The value must be the delivery-server origin without a trailing path.
# Only EXPO_PUBLIC_* values are bundled into the native app runtime.
# EXPO_PUBLIC_DELIVERY_SERVER_BASE_URL=https://delivery.example.com
