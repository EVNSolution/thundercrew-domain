package com.thundercrew.opsapi.rider.domain;

/**
 * 이용자의 직무. 260804 미팅 요구사항 — "이용자: 라이더/클리너 구분".
 *
 * <p>차량의 용도({@code BikePurpose})와 짝을 이룬다. 배송용 차량에는 라이더가,
 * 클린차량에는 클리너가 붙는다. 다만 이 단계에서는 배차 로직이 직무로 분기하지 않는
 * 서술 값이다 — 강제 규칙은 용도별 화면 분리가 들어올 때 붙인다.
 *
 * <p>한 사람이 두 직무를 겸하지 않는다고 본다. 겸직이 필요해지면 이 enum 이 아니라
 * 별도 테이블이 필요하다.
 */
public enum RiderRole {
    /** 배송 라이더. */
    RIDER,
    /** 클리너. */
    CLEANER
}
